build:
  docker:
    web: docker/Dockerfile
    flare: docker/Dockerfile.flaresolverr

release:
  image: web
  command:
    - uv
    - run
    - alembic
    - upgrade
    - head

run:
  web: uv run litestar --app hjudge.app:app run --host 0.0.0.0 --port $PORT
  flare: flaresolverr
